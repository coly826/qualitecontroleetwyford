<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Liste des utilisateurs</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light p-4">
  <div class="container">

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-3 gap-2">
      <h2 class="text-primary fw-bold">👥 Liste des utilisateurs</h2>

      <!-- Supprimer tous les utilisateurs -->
      <a href="/auth/users/delete-all"
         class="btn btn-danger"
         onclick="return confirm('⚠️ Cette action supprimera TOUS les utilisateurs. Continuer ?')">
        🗑️ Supprimer tout
      </a>
    </div>

    <div class="table-responsive shadow-sm rounded">
      <table class="table table-striped table-bordered align-middle">
        <thead class="table-dark text-center">
          <tr>
            <th>Nom</th>
            <th>Adresse</th>
            <th>Email</th>
            <th width="120">Actions</th>
          </tr>
        </thead>

        <tbody>
          <% if(users.length === 0){ %>
            <tr>
              <td colspan="4" class="text-center text-muted">
                Aucun utilisateur trouvé
              </td>
            </tr>
          <% } %>

          <% users.forEach(user => { %>
            <tr>
              <td><%= user.nom %></td>
              <td><%= user.adresse %></td>
              <td><%= user.email %></td>
              <td class="text-center">
                <!-- Supprimer un utilisateur -->
                <a href="/auth/users/delete/<%= user._id %>"
                   class="btn btn-danger btn-sm"
                   onclick="return confirm('Supprimer cet utilisateur ?')">
                  🗑️
                </a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <a href="/dashboard" class="btn btn-primary mt-3">
      ⬅ Retour au Dashboard
    </a>

  </div>

  <!-- Bootstrap JS Bundle (Popper inclus) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
