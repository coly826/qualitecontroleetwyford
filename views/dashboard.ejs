<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Twyford - QC Dashboard</title>

<!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-primary fixed-top">
  <div class="container">
    <a class="navbar-brand">TWYFORD QC DASHBOARD</a>

    <div class="d-flex gap-2">
      <a class="btn btn-light btn-sm" href="/form">Formulaire</a>
      <a class="btn btn-success btn-sm" href="/rapport">📊 Rapport Global</a>

      
      <a class="btn btn-warning btn-sm" href="/auth/users">👥 Utilisateurs</a>
      <a class="btn btn-outline-light btn-sm" href="/auth/logout">
        Se déconnecter
      </a>
    </div>
  </div>
</nav>

<div class="container mt-5 pt-4">
  <h4 class="text-primary mt-3">Historique des contrôles</h4>

  <% 
    const moisListe = ["Janvier","Février","Mars","Avril","Mai","Juin","Juillet","Août",
                       "Septembre","Octobre","Novembre","Décembre"]; 
  %>

  <% moisListe.forEach(mois => { %>
    <% let filtres = data.filter(d => d.mois === mois); %>
    <% if(filtres.length > 0){ %>

      <h5 class="mt-4 text-secondary border-bottom pb-1"><%= mois %></h5>

      <!-- Table responsive -->
      <div class="table-responsive">
        <table class="table table-bordered table-striped align-middle">
          <thead class="table-primary">
            <tr>
              <th>Date</th>
              <th>Groupe</th>
              <th>Cartons</th>
              <th>Pièces</th>
              <th>Étiquettes</th>
              <th>Edge</th>
              <th>Surface</th>
              <th>Broken</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% filtres.forEach(item => { %>
              <tr>
                <td><%= item.date %></td>
                <td><%= item.groupe %></td>
                <td><%= item.cartons %></td>
                <td><%= item.pieces %></td>
                <td><%= item.etiquettes %></td>
                <td><%= item.edge %></td>
                <td><%= item.surface %></td>
                <td><%= item.broken %></td>
                <td>
                  <a href="/delete/<%= item._id %>" class="btn btn-danger btn-sm">❌</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

    <% } %>
  <% }) %>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
